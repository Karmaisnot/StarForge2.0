services:
  console:
    build:
      context: .
      dockerfile: Dockerfile
    image: starforge-console:latest
    container_name: starforge-console
    ports:
      - "8080:80"
    restart: unless-stopped
